# Slice B — Branch-and-compare scaffold (C-005)

**Status:** Done — machine (C-005 Lock remains owner)  
**Register:** C-005 Open; P-005 Locked; T-001; T-006; C-004 Locked; N-002  
**New Process?** no — infrastructure + presentation (D-007 exempt)

## Steal

THESIS §7 / EXTERNAL_REFERENCES: no prior art forks a world under two force regimes. Habitat's original move. Mechanism = P-005 save restore + force dial snapshot (Slice F). Rejected: scoring / best-run leaderboard (N-002); dual viewport required for scaffold; inventing season dial in this slice.

## Rule

```
BranchDocument = SaveDocument + ForceSettings + width/height + isolation
fork → materialize A and B from one capture
stepBoth: A.stepEvent(); B.stepEvent()   // fair temporal compare
same forces → hash(A) = hash(B)
different rain → hash diverges; moisture encoding > 0.15
Compare tint = soil moisture (active − other) via elev-change encoder (T-006 observer)
```

## Physics feedback

None new. Forces already drive atmosphere / sea / tide / wind. Branch only isolates trajectories.

## Paired expectation

Same castle, heavy vs dry rain → wetter branch shows cooler moisture tint under Compare; identical force forks stay bit-identical. Probe: `branch-compare`.

## Bans

Optimization surface / scoreboard (N-002) · ecological rewind as undo (S-007 / C-013) · cell-targeted forces (THESIS §9) · number-reading HUD as the compare payoff
